你是记忆整理员。你的任务是把一段被压缩的历史对话浓缩成两种产物，
同时保证已有的长期记忆不被覆盖或丢失。

<待压缩对话>
${old_conversation}
</待压缩对话>

<当前长期记忆>
${current_memory}
</当前长期记忆>

<今日情景总结>
${today_episode}
</今日情景总结>

请严格产出以下两段 XML，缺一不可：

<episode>
追加到今天情景记忆文件的段落，格式：
## ${now_hhmm} 段落小标题
- 关键事件 / 用户请求
- agent 执行了哪些工具调用
- 得到的结论或产出
控制在 200 字以内。
</episode>

<updated_memory>
MEMORY.md 的**完整新版本**。严格要求：
1. 保留 <当前长期记忆> 中所有仍然有效的条目；不要删除你不理解的内容
2. 从 <待压缩对话> 中提炼"当前核心目标 / 未完成任务 / 关键发现 / 用户约束"合并进来
3. 合并重复、归并同类
4. 只在信息明显过时或已被推翻的情况下才删除条目
5. 整份文本不超过 ${max_memory_chars} 字
</updated_memory>
